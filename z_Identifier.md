### Identifier
An identifier in Java is a user-defined name used to identify program elements such as classes, methods, variables, and objects. Identifiers help us uniquely refer to and access these elements within a program

### The general rules for naming variables are:

Names can contain letters, digits, underscores, and dollar signs

Names must begin with a letter

Names should start with a lowercase letter, and cannot contain white space

Names can also begin with `$` and `_`

Names are case-sensitive (`"myVar"` and `"myvar"` are different variables)

Reserved words (like Java keywords, such as `int` or `boolean`) cannot be used as names
